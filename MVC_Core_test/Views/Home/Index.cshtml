@model MVC_Core_test.ViewModels.HomeViewModel

@{
    ViewData["Title"] = "Index";
}

<h1>@Model.Title</h1>

@foreach (var pie in Model.Pies)
{
    <div>
        <h2>@pie.Name</h2>
        <h3>@pie.Price</h3>
    </div>
}

